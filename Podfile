# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

# share pods
def shared_pods
  use_frameworks!
  pod 'Kanna', '~> 5.0.0'
  pod "Kingfisher"
end

# iOS targets
target 'Unam' do
  shared_pods
end

target 'UnamUITests' do
  shared_pods
end
